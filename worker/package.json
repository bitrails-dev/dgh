{
  "name": "hospital-cms-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute hospital-cms --local --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute hospital-cms --remote --file=src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250501.0",
    "wrangler": "^4.14.0",
    "typescript": "^5.8.0"
  }
}
